# 不进 Docker build context 的文件
# 减少 context 大小 + 避免无关文件变化污染 build cache

# 本地配置 (含敏感信息, 不能进镜像)
.env
.env.*
!.env.example
.env.knodo-backup

# git
.git
.gitignore

# 部署/测试用文件, 跟镜像无关
docker-compose.yml
ovscs.pod.yaml
README.md

# 主机端工具 (Windows 脚本, 不在容器里跑)
# 注释: extract-knodo-credentials.sh 进容器 (允许 docker exec 提取 BACKEND_WS_URL+API_TOKEN)
scripts/backup-docker-mirrors.ps1

# docker 配置备份目录
.docker-config-backup/
