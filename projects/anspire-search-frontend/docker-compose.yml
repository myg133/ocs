# =============================================================================
# anspire-search-frontend 项目部署
# 基于 base image: myg133/openvscode-server:njs-knodo
# base 镜像更新只需 docker pull myg133/openvscode-server:njs-knodo + restart
# 无 Dockerfile, 项目目录只有 compose + env 配置
# =============================================================================

services:
  openvscode-server:
    image: myg133/openvscode-server:njs-knodo
    container_name: ocs-anspire-search-frontend
    restart: unless-stopped
    # host 端口不暴露, 外部访问全走 Traefik (search.localdev.anspire.cn)
    # 调试: docker exec -it ocs-anspire-search-frontend bash
    #       docker exec ocs-anspire-search-frontend curl 127.0.0.1:9910/health
    environment:
      # ---- knodo 凭证 (entrypoint.sh 三选一) ----
      - KNODO_INSTALL_URL=${KNODO_INSTALL_URL:-}
      - BACKEND_WS_URL=${BACKEND_WS_URL:-}
      - API_TOKEN=${API_TOKEN:-}
      - KNODO_AGENT_PORT=9910
      # === 代理 env vars: 大写在 .env 配 (source of truth), 小写自动引用大写 ===
      # base image 烤了 HTTP_PROXY=http://172.25.93.8:10808 (公司代理,慢, 155 KB/s)
      # docker-compose 把 .env 注入的大写 env vars 直接覆盖 base image 烤的
      # 小写 (https_proxy) 用 ${HTTPS_PROXY} 引用大写, 大小写始终一致, curl 直连
      # 默认 .env 里 HTTPS_PROXY/HTTP_PROXY/NO_PROXY 都留空, 容器里 6 个 env vars 全是空 → 直连 (1.12 MB/s)
      - HTTPS_PROXY=${HTTPS_PROXY:-}
      - https_proxy=${HTTPS_PROXY:-}
      - HTTP_PROXY=${HTTP_PROXY:-}
      - http_proxy=${HTTP_PROXY:-}
      - NO_PROXY=${NO_PROXY:-}
      - no_proxy=${NO_PROXY:-}
    volumes:
      # knodo 状态 (NFS, 替代 named volume)
      - knodo-state:/home/workspace/.knodo
      # 项目代码 (NFS)
      - project-data:/home/workspace/project
    # ---- Traefik 路由 (走 traefik-net 共享网络) ----
    # 证书用 traefik.yml 里的 tls.certificates 静态加载 (acme.sh 签的 *.localdev.anspire.cn)
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.search.rule=Host(`${OCS_HOST:?OCS_HOST required}`)"
      - "traefik.http.routers.search.entrypoints=websecure"
      - "traefik.http.routers.search.tls=true"
      - "traefik.http.services.search.loadbalancer.server.port=3000"
    networks:
      - traefik-net
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:3000/healthz || exit 0"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

volumes:
  knodo-state:
    driver: local
    driver_opts:
      type: nfs
      o: addr=172.25.93.9,rw,sync,hard,intr,noatime
      device: ":/data-volumes/anspire-search-frontend/knodo"
  project-data:
    driver: local
    driver_opts:
      type: nfs
      o: addr=172.25.93.9,rw,sync,hard,intr,noatime
      device: ":/data-volumes/anspire-search-frontend/project"

networks:
  traefik-net:
    external: true
    name: traefik-net
