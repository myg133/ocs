# =============================================================================
# Traefik 全局环境变量
# 复制: cp .env.example .env
#
# cert 申请走 acme.sh (在服务器上手动跑, 不在 docker 容器内),
# 凭证是临时 export 到 shell, 不入 .env / 不入 git
# Traefik 容器自己只读 ./certs/wildcard.{fullchain.crt,key} 这一对静态 cert
#
# 各个 knodo 服务的 host 写在各自的 .env 里 (PY_KNODO_HOST / NJS_KNODO_HOST 等)
# =============================================================================

# ---- 必填: Traefik dashboard 域名 ----
# 浏览器通过这个域名看 Traefik dashboard (routers / services / certs 状态)
# 走 HTTPS, 走 ./certs/wildcard.fullchain.crt 静态 cert (acme.sh 申请的泛域)
DASHBOARD_HOST=traefik.localdev.anspire.cn

# ---- 泛域名前缀 (仅做注释参考, Traefik 不读) ----
# 在腾讯云 DNSPod 加 1 条泛域名 A 记录:
#   *.localdev  A  172.25.93.9
# 这样 py-knodo.localdev.anspire.cn / njs-knodo.localdev.anspire.cn 都能解析
# 各 knodo 容器的具体 host 名写到各自 .env 里
