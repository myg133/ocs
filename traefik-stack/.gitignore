# =============================================================================
# traefik-stack 专属排除
# - .env: 腾讯云 CAM 密钥
# - certs/: acme.sh 签发的 wildcard cert (server-side 落地, 不在 git)
# =============================================================================

.env
certs/
